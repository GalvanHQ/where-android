<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        Base activity theme. Compose owns colours, so this only needs to
        be a no-action-bar parent that renders edge-to-edge cleanly.
        windowBackground is set to splash_background so any pre-Compose
        flash matches the canvas.
    -->
    <style name="Theme.Where" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/splash_background</item>
        <item name="android:colorBackground">@color/splash_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <!-- Splash screen theme applied to MainActivity before Compose takes over -->
    <style name="Theme.Where.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_background</item>
        <item name="postSplashScreenTheme">@style/Theme.Where</item>
    </style>
</resources>
