<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        Dark-mode counterpart of Theme.Where. Keeps windowBackground in
        sync with the dark color scheme's surface so there's no white
        flash before Compose paints on a dark system.
    -->
    <style name="Theme.Where" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/splash_background_dark</item>
        <item name="android:colorBackground">@color/splash_background_dark</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <style name="Theme.Where.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background_dark</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/where_splashscreen_logo</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_background_dark</item>
        <item name="postSplashScreenTheme">@style/Theme.Where</item>
    </style>
</resources>
